---
title: Web UI primitives
description: The toast, dialog, modal, and tooltip primitives shared across the UI.
order: 3
---

This folder holds three pages: [toasts and dialogs](./toasts-and-dialogs.md), covering the `toast` store and `dialog.confirm` and `dialog.prompt`; [modals and overlays](./modals-and-overlays.md), covering the `Modal` component and the `Z_LAYERS` stacking order; and [popovers and tooltips](./popovers-and-tooltips.md), covering the Radix Popover wrapper and the single `TooltipHost`.
